apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-app.fullname" . }}
  labels:
    {{- include "grafana-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-app.selectorLabels" . | nindent 4 }}
